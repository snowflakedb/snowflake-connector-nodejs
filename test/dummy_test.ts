import assert from "assert";
import * as snowflake from "../lib/snowflake";

console.log('snowflake', snowflake);

describe('Dummy test', () => {
  it('passed', () => {
    assert.equal(1, 1);
  })
});
